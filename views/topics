include index
  .mainTopicDiv(ng-controller='topicController' ng-init='topic=#{topics}')
    h1 #{group_name}
    .topicDiv(ng-repeat='topic in topics')
      a(href='/groups/#{group_id}/flashcards/{{topic._id}}') {{ topic.title }}
    br
    a(href='/groups/#{group_id}/topics/new') Create a new topic
    
