doctype html
html(lang="en")
  head
    ng-app='app'
    title Finals Club
    script(type="text/css" src="../assets/css/fonts.css")      
    script(type="text/css" src="../assets/css/style.css")      
    script(src="../../bower_components/jquery/dist/jquery.min.js")
    script(src="../../js/app.js")
  // mainController ? 
  div.masthead 
  div.logbar
    div.loginstatus
      - if( user && user.loggedIn )
        | Currently logged in as 
        span.username
          | !{ user.displayName || user.name }
      - else
        | &nbsp;
  div.navbar
    table
      tr
        td
          a(href="/")
        td.menu
          a(href="/groups") Groups
          a(href="/archive") Archive
          a(href="/press") Press

          - if( user && user.loggedIn )
            a(href="/profile") Profile
            a.special( href='/logout' ) Logout
          - else
            a(href="/register") Create an Account
            a.special(href="/login") Login
  div.logbar
    //- &nbsp;
  ng-view=''
// place view here!

